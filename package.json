{
  "name": "mzazi-link",
  "version": "2.0.0",
  "private": true,
  "description": "MZAZI TECH — public, login-free WhatsApp device linking. Pairs through the shared Neon bot_control registry and reads live device status from bot_status, so it needs no bot-side changes.",
  "homepage": "https://link.mzazi.shop",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "db:init": "node scripts/init-db.mjs"
  },
  "dependencies": {
    "next": "14.2.35",
    "pg": "^8.12.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
